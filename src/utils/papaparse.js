// src/utils/papaparse.js
// Import or require papaparse.js for CSV parsing
// You may need to install papaparse via npm/yarn for a modern build
// Example: npm install papaparse

import Papa from 'papaparse';
export default Papa;
